import Room from './Room';
import formatLog from '../../util/formatLog';

class SecondFloorLanding extends Room {
  description(): void {
    formatLog('PLACEHOLDER');
  }
}

export default SecondFloorLanding;
